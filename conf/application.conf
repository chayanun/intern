# https://www.playframework.com/documentation/latest/Configuration
#play.filters.headers.contentSecurityPolicy="script-src 'self' 'unsafe-inline' 'unsafe-eval'"

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
#
# This must be changed for production, but we recommend not changing it in this file.
#
# See http://www.playframework.com/documentation/latest/ApplicationSecret for more details.
#play.crypto.secret = "changeme"
play.http.secret.key="9FXvQy7tmoE2kvjktMWrYIIBoQAGVoYl"
play.http.secret.key="%APPLICATION_SECRET%"

play.filters.enabled = []

#play.filters.hosts {
#  allowed = ["."]
#}
# The application languages
# ~~~~~
play.i18n.langs = ["en"]

# Database configuration
# ~~~~~
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
#
slick.dbs {
  default.profile = "slick.jdbc.PostgresProfile$"
  default.db.driver = "org.postgresql.Driver"
  default.db.url = "jdbc:postgresql://localhost:5432/interndb?currentSchema=intern"
  default.db.user = "internuser"
  default.db.password = "u123456"
}

mailgun.api.key = "6eaf7a3e8a82630e430b9eda0b5eb11b-0470a1f7-77d9cb37"
mailgun.api.url = "https://api.mailgun.net/v3/sample.intern.com/messages"
mailgun.default.sender = "service@sample.intern.com"
